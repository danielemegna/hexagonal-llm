[tool.poetry]
name = "aifoodfacts"
version = "0.1.0"
description = "Open Food Facts with AI"
authors = ["danielemegna"]
package-mode = true

[tool.poetry.scripts]
main = "aifoodfacts.main:main"

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"